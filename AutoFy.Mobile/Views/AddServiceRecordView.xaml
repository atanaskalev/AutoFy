<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AutoFy.Mobile.Views.AddServiceRecordView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Сервизна дейност"
    BackgroundColor="#F3F4F6">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="18">

            <VerticalStackLayout Spacing="4">
                <Label
                    Text="Добави сервизна дейност"
                    FontSize="30"
                    FontAttributes="Bold"
                    TextColor="#111827" />

                <Label
                    Text="{Binding VehicleName}"
                    FontSize="14"
                    TextColor="#6B7280" />
                
                <Label
                    Text="Запиши ремонт, обслужване или друга дейност по автомобила."
                    FontSize="14"
                    TextColor="#6B7280" />
            </VerticalStackLayout>

            <Border
                BackgroundColor="White"
                StrokeThickness="0"
                StrokeShape="RoundRectangle 24"
                Padding="16">

                <VerticalStackLayout Spacing="16">

                    <Entry
                        Placeholder="Име на сервиз / майстор"
                        Text="{Binding ServiceName}" />

                    <Entry
                        Placeholder="Локация"
                        Text="{Binding ServiceLocation}" />

                    <Picker
                        Title="Тип дейност"
                        ItemsSource="{Binding ServiceTypes}"
                        SelectedItem="{Binding SelectedServiceType}" />

                    <Entry
                        Placeholder="Километраж"
                        Keyboard="Numeric"
                        Text="{Binding Odometer}" />

                    <Entry
                        Placeholder="Цена"
                        Keyboard="Numeric"
                        Text="{Binding Price}" />

                    <DatePicker
                        Date="{Binding ServiceDate}" />

                    <Editor
                        Placeholder="Описание / бележка"
                        AutoSize="TextChanges"
                        HeightRequest="120"
                        Text="{Binding Notes}" />

                </VerticalStackLayout>
            </Border>

            <Button
                Text="Запази дейността"
                HeightRequest="54"
                CornerRadius="18"
                FontSize="16"
                FontAttributes="Bold"
                BackgroundColor="#111827"
                TextColor="White"
                Command="{Binding SaveServiceCommand}" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>